[project]
name = "pydspnel"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Loïc Fejoz", email = "loic@fejoz.net" }
]
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.0",
    "rply>=0.7.8",
]

[build-system]
requires = ["uv_build>=0.8.18,<0.9.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
